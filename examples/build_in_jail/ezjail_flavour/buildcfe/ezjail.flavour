#!/bin/sh

exec >/log.flavour 2>&1
ASSUME_ALWAYS_YES=YES pkg
/usr/local/sbin/pkg2ng
pkg install -y security/sudo
pkg install -y devel/git
pkg install -y devel/autoconf
pkg install -y devel/automake
pkg install -y devel/libtool 
pkg install -y devel/gmake
pkg install -y devel/cmake
pkg install -y devel/bison
